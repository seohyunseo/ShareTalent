<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Board">
<insert id="insertBoard">
		insert into TBOARD (title, category, writer, content, rate, fee)
		values
		(#{title} , #{category} , #{writer} , #{content}, #{rate}, #{fee})
	</insert>
	<update id="updateBoard">
		update TBOARD
		set title=#{title}, content=#{content}, writer=#{writer},
		category=#{category}, rate=#{rate}, fee=#{fee}
		where seq=#{seq}
	</update>
	<delete id="deleteBoard">
		delete from TBOARD where seq=#{seq}
	</delete>
	<select id="getBoard" resultType="board">
		select * from TBOARD where seq=#{seq}
	</select>
	<select id="getBoardList" resultType="board">
		select * from TBOARD order by seq desc
	</select>
</mapper>